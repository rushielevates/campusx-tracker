# Local Credentials

Put local-only environment variables in `.env` inside this folder.

Render should be configured with the same variable names in its own Environment settings:

- `MONGODB_URI`
- `SESSION_SECRET`
- `YOUTUBE_API_KEY`

The local `.env` file is ignored by git.
